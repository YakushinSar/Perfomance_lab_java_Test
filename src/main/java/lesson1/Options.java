package lesson1;

public class Options {
    /*
ChromeOptions options = new ChromeOptions();
options.addArguments("--start-maximized"); - Запускает браузер в развёрнутом окне
options.addArguments("--incognito"); - Режим инкогнито (не сохраняет историю, кэш, cookies)
options.addArguments("--disable-notifications"); - Отключает уведомления браузера
options.addArguments("--headless"); - Запускает браузер в фоновом режиме (без отображения окна)
options.addArguments("--disable-gpu"); - Отключает GPU (часто используется с headless)
options.addArguments("--window-size=1920,1080"); - Устанавливает размер окна
options.addArguments("--lang=ru"); - Устанавливает русский язык интерфейса
options.addArguments("--disable-blink-features=AutomationControlled"); - Скрывает факт автоматизации (чтобы сайты не определяли WebDriver)
WebDriver driver = new ChromeDriver(options);
     */
}
